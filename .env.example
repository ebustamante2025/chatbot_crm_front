# URL del backend para API (proxy en dev usa /api -> backend)
# VITE_API_URL=http://localhost:3004

# URL del backend para WebSocket. Si el proxy cierra la conexión ws, usa el puerto del backend:
VITE_WS_URL=http://localhost:3004
