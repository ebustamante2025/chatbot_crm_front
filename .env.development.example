# =============================================================================
# Solo Vite en localhost: copiar a .env.development → npm run dev
# VITE_API_URL define también el destino del proxy (/api, /socket.io): mismo host:puerto que el API.
# =============================================================================

VITE_API_URL=http://localhost:3004/api
# VITE_WS_URL=http://localhost:3004
